# 共享残差混合专家 G1/G2 不变量结论

- 记录时间：2026-08-12 18:32
- 驱动：`docs/archive/drivers/20260812-1807-共享残差混合专家-函数保持与持续学习-驱动.md`
- 证据：`cache/audit/shared_residual_continual/shared_residual_experiment_invariants.json`
- 生命周期：G1=`done`，G2=`done`，G3=`not-started`
- Gate verdict：G1=`PASS`，G2=`PASS`，G3=`NOT_EVALUATED`
- 强主张状态：`NOT_SUPPORTED`（G1/G2 只验证实现，不证明持续学习收益）

## 1. G1 Function-Preserving Dense-to-MoE Upcycling

| seed | logits max abs error | loss abs error | probe AUC abs error | shared copy | specialist zero | router uniform |
|---:|---:|---:|---:|---|---|---|
| 42 | `7.1526e-07` | `0` | `0` | PASS | PASS | PASS |
| 123 | `2.3842e-07` | `0` | `0` | PASS | PASS | PASS |
| 456 | `4.7684e-07` | `0` | `0` | PASS | PASS | PASS |

预注册阈值：logits `≤1e-6`、loss `≤1e-7`、AUC `≤1e-12`。三 seed 全部通过，G1=`PASS`。

## 2. G2 Learning-Rate Semantics Invariant Gate

| 检查 | 结果 | 判定 |
|---|---:|---|
| pre-Adam 常数缩放实际 update ratio | `1.0` | PASS，验证 Adam 稳态抵消 |
| parameter-group 10× LR 实际 update ratio | `9.9982` | PASS |
| 当前 specialist 更新 | changed | PASS |
| 非拥有 specialists | 逐位不变 | PASS |
| 非活动 task head | 逐位不变 | PASS |
| 正式 task-head LR | `5e-4` | PASS |
| 正式 scanned specialist LR fixture | `2e-4` | PASS |
| sample-weighted gradient abs error | `0` | PASS |
| BWT fixture | `-0.1` | PASS |
| standard positive forgetting fixture | `+0.1` | PASS |

G2=`PASS`。这只授权执行驱动中已经冻结的 24 次 G3 矩阵；不授权 shared LR、router LR、完整 continual baseline 或 sparse scaling。

## 3. 当前可执行状态

- 24-trial 计划已机械验证；
- 固定 `seed_device_map`：42/456→`cuda:0`，123→`cuda:1`；
- 同卡单 trial 串行，不同 seed 跨卡并行；
- dense checkpoint SHA-256 已逐 seed 同时绑定 G1、matrix 和 trial；
- 单 trial 失败后继续下一项；矩阵级失败终止全部活跃子进程；
- 重入只接受 provenance、配置和产物哈希全部一致的 `done` trial；
- 汇总失败不能把 matrix 标为成功；
- 当前未启动 GPU 训练，G3=`not-started`。

执行命令：

```bash
bash scripts/matrix/shared_residual_experiment.sh start
```

状态命令：

```bash
bash scripts/matrix/shared_residual_experiment.sh status
```
