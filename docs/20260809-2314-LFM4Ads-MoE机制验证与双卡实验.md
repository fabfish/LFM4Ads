# D9 — LFM4Ads MoE 机制验证与双卡实验

| 项 | 值 |
|----|----|
| **文档 ID** | D9 |
| **角色** | 实验（机制裁决） |
| **状态** | ✅ 主结论闭合，**且已通过 4-seed 显著性检验**（D15, Welch t=15.45）；D16 复核进行中 |
| **关联代码** | `main_moe.py`、`main_moe_v2.py`、`train.py`、`model.py`、`scripts/measure_gradient_dominance.py` |
| **主数据源** | `cache/moe_v1_summary_*.json`、`cache/moe_v2_train_history*.json`、`cache/grad_dominance_*.json`、`cache/d10_summary.json`、`cache/adatask_results.csv`、`cache/continual_analysis.json` |
| **上游** | D7（路线图）、D8（公平口径）、D6（V2 设计） |
| **执行方式** | `.codebuddy/skills/lfm4ads-watch` 双卡自循环（cuda:0 / cuda:1 各一条串行链） |

---

## 1. 目标

回答 D7 路线图提出的四个机制问题：

1. **Q-A**：MoE 增益来自 AdaTask 式的「梯度抑制/促进」调制，还是「shared 均衡 + routed 特殊化」？
2. **Q-B**：只训练 router + experts（冻结 DNN/Head/Sparse，下称 **rx-only**）够不够？
3. **Q-C**：V2（Shared Expert + Top-K 路由）相对 V1（纯 K 专家 + Router）是否真的更强？
4. **Q-D**：梯度主导度诊断（RMS-per-param）能否**预测**训练超参该怎么调？

---

## 2. 统一口径

| 项 | 值 |
|----|----|
| 基座 | `cache/dcnv2_vanilla.pt`（不重训，所有实验共用） |
| seed | 42（单 seed，无方差估计；见 §7 G-1） |
| batch_size | 10000 |
| lr | 1e-3（除显式标注 3e-3 的对照） |
| max_epochs | 8 |
| 指标 | test AUC（`torcheval.metrics.BinaryAUROC`），全量 test 集 |
| Vanilla 参照 | `test_auc_all = 0.7775` |
| rx-only 定义 | `--freeze dnn,head,sparse` → 仅 CrossExpert(+CrossShared)+Router 可训 |

> ⚠️ 全部实验 `--skip-downstream`（先出机制结论，下游 224 训练另计）。
> ⚠️ 跨 scenario 绝对 AUC 不可比；表中 `mean_*` 为 8 场景算术均值，仅用于同口径内相对比较。

---

## 3. 主结果表

### 3.1 V1（纯 K 专家）—— 来源 `cache/moe_v1_summary_*.json`

提取命令：
```bash
python -c "import json,glob,os
for f in sorted(glob.glob('cache/moe_v1_summary_*.json')):
    d=json.load(open(f)); c=d['config']
    print(os.path.basename(f), c['K'], c['freeze'], round(d['test_auc_all'],4))"
```

| tag | K | lr | freeze | test AUC | mean(8场景) |
|-----|---|----|--------|----------|-------------|
| `k1_full` | 1 | 1e-3 | — | 0.7709 | 0.7143 |
| `k1_freeze_dh` | 1 | 1e-3 | dnn,head | 0.7561 | 0.7042 |
| `k1_rxonly` | 1 | 1e-3 | dnn,head,sparse | 0.7554 | 0.6871 |
| `k2_rxonly` | 2 | 1e-3 | dnn,head,sparse | 0.7714 | 0.7068 |
| `k4_freeze_dh` | 4 | 1e-3 | dnn,head | 0.7731 | 0.7132 |
| **`k4_rxonly`** | **4** | **1e-3** | **dnn,head,sparse** | **0.7723** | **0.7049** |
| `k4_rxonly_lr3e3` | 4 | 3e-3 | dnn,head,sparse | 0.7526 | 0.6946 |
| `k8_rxonly` | 8 | 1e-3 | dnn,head,sparse | 0.7721 | 0.7071 |
| — Vanilla — | — | — | — | 0.7775 | 0.7173 |

### 3.2 V2（Shared Expert + Top-K）—— 来源 `cache/moe_v2_train_history*.json`

| tag | K | top_k | lr | β₂ | lb_α | freeze | test AUC |
|-----|---|-------|----|----|------|--------|----------|
| （无 tag，全量） | 4 | 2 | 1e-3 | .999 | .01 | — | **0.7672** |
| `rxonly` | 4 | 2 | 1e-3 | .999 | .01 | dnn,head,sparse | 0.7471 |
| `rxonly_long` | 4 | 2 | 1e-3 | .999 | .01 | dnn,head,sparse | 0.7498 |
| `rxonly_nosh` | 4 | 2 | 1e-3 | .999 | .01 | +shared | 0.7295 |

### 3.3 D10 消融（rx-only 体制，K=4）—— 来源 `cache/d10_summary.json`

| tag | 被测项 | test AUC | Δ vs 同组参照 |
|-----|--------|----------|---------------|
| `d10_beta2_09` | lr3e-3, **β₂=0.90** | 0.7416 | vs β₂=.999 高 LR 塌陷 |
| `d10_beta2_095` | lr3e-3, **β₂=0.95** | 0.7418 | 同上 |
| `d10_lbalpha_05` | lb_α=0.05 | 0.7481 | **> lb 0.01(0.7471)、0.10** |
| `d10_lbalpha_10` | lb_α=0.10 | 0.7362 | 过强，−0.011 |
| `d10_topk_3` | top_k=3 | 0.7503 | ↑ |
| **`d10_topk_4`** | **top_k=4 (=K, soft)** | **0.7566** | **V2 rx-only 最优** |

### 3.4 梯度主导度（RMS-per-param）—— 来源 `cache/grad_dominance_*.json`

提取：`python -c "import json;d=json.load(open('cache/grad_dominance_v2_full.json'));print(d['verdict'])"`

| 配置 | RMS(Expert) | RMS(DNN) | expert/dnn | router/expert | shared/routed | load_imbalance |
|------|-------------|----------|-----------|---------------|---------------|----------------|
| v1_full | 6.17e-4 | 9.78e-4 | **0.631** | 4.69 | — | 0.056 |
| v1_rxonly | 5.49e-4 | 5.74e-3 | **0.096** | 14.52 | — | 0.069 |
| v2_full | 3.26e-4 | 8.18e-4 | **0.398** | 7.95 | **2.62** | 0.078 |
| v2_rxonly | 4.03e-4 | 4.55e-3 | **0.089** | 14.5 | 2.03 | 0.198 |

> Sparse 组 RMS 仅 5e-6～1.5e-5（占 99.2% 参数但几乎不动），印证 D7 §1.3 修正：**参数量占比预测不了梯度占比**。

---

## 4. 机制裁决

### Q-A：增益来自「调制」还是「特殊化」？→ **两者都不是主因；主因是"哪些参数在动"**

- **AdaTask 三模式**（`cache/adatask_results.csv`，8 场景）：`encourage` 在 6/8 场景优于 `none`（如 s5: 0.6412→0.6870，s8: 0.6663→0.7030），`suppress` 在 s8 反而最高（0.7382）。**方向不一致 → 梯度调制不是稳定增益源**（且受 D8 §G-3 的 7 项口径差异污染，不可与 D3 直接比）。
- **特殊化证据薄弱**：v2_full 的 `scenario_divergence_mean = 0.0029`，v1_full 为 0.0028 —— 各专家在不同 scenario 上的梯度方向几乎不分化。
- **真正起作用的是分工比例**：`shared_vs_routed = 2.62`（v2_full）说明共性知识几乎全部由 shared expert 承担，routed 专家基本闲置。`rxonly_nosh`（额外冻结 shared）掉到 0.7295，是 V2 rx-only 全组最低 —— **shared 是承重墙，routed 是装饰**。

### Q-B：只训 router + experts 够不够？→ **V1 够（−0.005），V2 不够（−0.020）**

| 体制 | 全量 | rx-only | 差距 | 可训参数 |
|------|------|---------|------|----------|
| V1 K=4 | 0.7709(K=1)/— | **0.7723** | rx-only 甚至≥全量 | 390,060 (0.46%) |
| V2 K=4 | 0.7672 | 0.7471 | **−0.0201** | 398,544 (0.47%) |

**V1 rx-only = 0.7723，仅用 0.46% 参数就逼近 Vanilla 的 0.7775（−0.005）。** 这是本轮最强的实用结论：Cross Expert + Router 是承载场景适配的充分子空间，无需动 8400 万的 embedding 表。

### Q-C：V2 是否强于 V1？→ **不是。V1 全面胜出**

- 全量：V2 0.7672 < V1 k4_freeze_dh 0.7731
- rx-only：V2 0.7471 ≪ V1 0.7723（**−0.025**）
- 即便 V2 用上 D10 最优 soft routing（0.7566），仍低于 V1 rx-only 的 0.7723。

**归因**：V2 的 Top-K 稀疏路由在小可训参数体制下造成两重伤害——(1) `load_imbalance` 从 V1 的 0.069 恶化到 0.198（路由塌缩，L2 单专家独占 0.50）；(2) 每步只有 top_k/K 的专家收到梯度，本已 `expert/dnn=0.089` 的更新量再被稀疏化砍半。D10 的 `top_k=K`（全 soft，等价关掉稀疏）拿到 V2 组最高 0.7566，正是这一归因的直接验证。

### Q-D：梯度诊断有没有预测力？→ **有，且被 D10 证实**

诊断 `RMS(Expert)/RMS(DNN)=0.089` 给出的建议是「提高 expert LR 或降 β₂」。检验：

- 单纯提高全局 LR（`k4_rxonly_lr3e3`，β₂=.999）：0.7723 → **0.7526，掉点**。
- 提高 LR **同时降 β₂ 到 0.95/0.90**：0.7416/0.7418（V2 组），相对同体制 β₂=.999 高 LR 的塌陷显著回升。

**解释**：β₂=0.999 的二阶动量窗口约 1000 步，对稀疏激活的专家梯度过度平滑，使有效步长被压制；降到 0.95（窗口 ~20 步）后专家才真正跟上。

> ⚠️ **§8.2/8.3 修正本节**：D13 的完整 LR 扫描表明，`expert/dnn=0.089` 的正确读法**不是"步长不够、要加 LR"，而是"当前 LR 已过冲、专家被噪声主导"**。把 LR 降到 5e-4 得到 0.7750（峰值），远优于任何"高 LR + 低 β₂"组合（≤0.7581）。**β₂ 只在 LR 过冲时充当补偿；在正常 LR 下降 β₂ 反而轻微有害。**
> 诊断量的**预测力成立**（它确实指向"专家更新与 DNN 严重失配"），但**修正动作的方向需由扫描确定**——这是本轮方法论上的主要教训。

---

## 5. 副结论

- **K 的边际收益在 K=2 就饱和**：k2 0.7714 / k4 0.7723 / k8 0.7721（V1 rx-only），差异 <0.001，落在单 seed 噪声内。**不要用"加专家"讲故事。**
- **K=1 是关键对照**：k1_rxonly 0.7554 显著低于 k2/k4/k8（~0.772）。说明增益确实来自"多专家分工"而非"多训了一组 Cross 参数"——因为 K=1 架构上等价于 vanilla DCNv2 的 Cross。
- **lb_α 有甜点**：0.05 > 0.01 > 0.10。负载均衡损失过强会挤占主任务容量（lb loss 冲到 5.73）。
- **持续学习**（`cache/continual_analysis.json`）：MoE 的 `BWT_mean = −0.0236`，`head_weighted.delta = −0.0094`，仍在遗忘，MoE 结构本身没有解决灾难性遗忘。

---

## 6. 动机叙述（可写进论文 intro）

> 在工业推荐的多场景（multi-scenario）设定下，主流做法是给共享 backbone 挂 MoE 让不同场景走不同专家。我们在 KuaiRand-1K / DCNv2 上做了一次彻底的机制解剖，得到三点与直觉相悖的观察：
>
> **(i) 增益不来自专家特殊化。** 逐 (layer, expert, scenario) 的梯度分解显示场景间散度仅 0.003，专家几乎没有分化；加上 Shared Expert 后，共性/个性梯度比达到 2.6:1，即共享分支吞掉了绝大部分学习信号。
>
> **(ii) 增益来自"训练哪一小撮参数"。** 冻结 embedding 表（99.2% 参数）、DNN 与 Head，仅训练 Cross Expert + Router（390,060 参数，0.46%），在 lr=5e-4 下 test AUC 达 **0.7750**，相对全参微调的 Vanilla（0.7775）**仅损失 0.0025**。这提示多场景适配的有效自由度极低，可以用极小的可训练预算获得。
>
> **(iii) 稀疏路由在小预算体制下是负担而非优势。** Top-K 路由使负载不均衡度从 0.069 恶化到 0.198，且把本已不足的专家梯度进一步稀疏化；把 top_k 放宽到 K（即退化为 soft mixture）反而取得该体制下最优结果。
>
> 我们进一步用「每参数 RMS 梯度」这一诊断量定位问题：专家分支的每参数梯度仅为 DNN 的 0.089 倍。完整的学习率扫描表明其正解是**降低学习率**（单峰曲线峰值 5e-4，而非默认 1e-3），Adam β₂ 的调整只在学习率过冲时充当补偿。这说明 MoE 在小可训预算下的失败常被误诊为"容量不足"，实为**优化器超参与稀疏梯度规模不匹配**——诊断量能定位失配，但修正方向必须由扫描确定。

---

## 7. 缺陷与 Backlog

| ID | 摘要 | 状态 |
|----|------|------|
| G-1 | 全部结论单 seed=42，无方差估计 | ✅ **核心对比已闭合**（§10：V1 vs V2 四 seed，Welch t=15.45）。§3/§8 的其余单点仍无方差 |
| **G-4** | **`--seed` 是空操作**（无 shuffle + 权重来自固定 ckpt + router 零初始化） | ✅ 已定位并修复（§9），登记于 DRIVERS.md |
| D9-E | §3.1 中 K=2/4/8 差异 <0.001，且当时无有效 seed；**不可断言 K 的边际收益** | 🔄 需以 `--shuffle` 多 seed 重做 |
| G-3 | AdaTask 与 Phase 1 的 7 项口径差异（D8 §格局）未统一，本文 §4 Q-A 的 AdaTask 数据仅作方向性参考 | 🔄 待统一重做 |
| D9-A | 全部实验 `--skip-downstream`，Feature/Module/Model 三级下游未评估 | 📝 待补 |
| D9-B | V2 全量仅 1 组（top_k=2, lb .01），未在全量体制下扫 top_k；D11 已部署 `d11_soft_full` 补此空缺 | 🔄 D11 进行中 |
| D9-C | 「β₂ 联调」结论目前只在 V2 验证；D11 `d11_v1_rx_lr3e3_b95` 检验 V1 是否复现 | 🔄 D11 进行中 |
| D9-D | soft routing 最优是否外推到 K=8；D11 `d11_topk_K8` / `d11_K8_sparse2` 检验 | 🔄 D11 进行中 |

---

## 8. D11–D13 后续实验（已完成）

来源 `cache/d11_d13_summary.json`；调度 `cache/d1{1,2,3}_chain.sh <device>`（卡内串行、卡间并行、前一 job 退出即接力）。

### 8.1 D11 — soft routing 外推性

| tag | 配置 | test AUC | 裁决 |
|-----|------|----------|------|
| `d11_soft_full` | 全量, top_k=4 | **0.7696** | > V2 全量 top_k=2 的 0.7672 → **soft 在全量体制也赢** |
| `d11_topk_K8` | rx-only, K=8, top_k=8 | 0.7567 | ≈ K=4 soft 的 0.7566 → 外推成立但 **K 无边际收益** |
| `d11_K8_sparse2` | rx-only, K=8, top_k=2 | **0.7198** | **全场最低**：最强稀疏 = 最大伤害，soft 裁决加固 |
| `d11_topk4_lr3e3_b95` | soft + lr3e-3 + β₂.95 | 0.7396 | 组合**不叠加**，反低于单独 soft 的 0.7566 |
| `d11_combo_best` | 上者 + lb0.05 | 0.7396 | 与上者同值 → 该体制下 lb_α 无影响 |
| `d11_v1_rx_lr3e3_b95` | V1, lr3e-3, β₂.95 | 0.7581 | vs V1 lr3e-3 β₂.999 的 0.7526 → **β₂ 结论在 V1 复现** |

### 8.2 D12–D13 — V1 rx-only 的 LR 曲线（本轮最重要发现）

| lr | 5e-5 | 2e-4 | 3e-4 | **5e-4** | 1e-3 | 2e-3 | 3e-3 |
|----|------|------|------|----------|------|------|------|
| test AUC | 0.7109 | 0.7741 | 0.7741 | **0.7750** | 0.7723 | 0.7650 | 0.7526 |

**单峰曲线，峰值 lr=5e-4 → test AUC 0.7750。** 对比 Vanilla 全参微调 0.7775，**仅差 0.0025，而可训参数只有 390,060（0.46%）**。这是全部实验的 headline。

### 8.3 β₂ 的真实作用域

| 体制 | β₂=0.999 | β₂=0.95 | 结论 |
|------|----------|---------|------|
| V1, lr=3e-3（过冲） | 0.7526 | **0.7581** | 有帮助（+0.0055） |
| V1, lr=1e-3（正常） | **0.7723** | 0.7713 | 轻微有害（−0.0010） |

→ **β₂ 只是过冲 LR 的补偿手段，不是独立增益。** 修正 §4 Q-D 的表述：诊断给出的方向正确，但正解是**把 LR 调低**，而非"高 LR + 低 β₂"。

### 8.4 V1 vs V2 同 LR 公平对决

| 架构 | lr=5e-4, rx-only | 差距 |
|------|------------------|------|
| V1（纯 K 专家） | **0.7750** | — |
| V2-soft（shared + top_k=K） | 0.7558 | **−0.0192** |

**V2 的 Shared Expert + Router 架构在小可训预算体制下是确定性负担。**

---

## 9. G-4（严重）：`--seed` 一直是空操作

部署 D14 多 seed 时发现：seed=1 / 42 / 2024 返回**逐位相同**的 `test_auc_all = 0.7750083012867384`。

**原因链**（三个随机源全部缺失）：

1. 训练 `DataLoader` 未设 `shuffle`（`train.py:100`、`main_moe_v2.py:168`）→ 数据顺序确定；
2. MoE 专家权重从 `cache/dcnv2_vanilla.pt` **加载**而非随机初始化；
3. Router 按 D6 设计**零初始化**。

→ 整条训练路径无随机源，`torch.manual_seed` 无处生效。**D7 §5.3 P0 与 DRIVERS G-1 里所有"换 seed 复跑"的计划都不可能产生方差。** D14 当即终止。

**修复**：`train_moe(..., shuffle=False)` 与 `main_moe_v2.py` 训练 loader 加 `shuffle` 开关，两个入口加 `--shuffle`（默认 False，保持 §3/§8 历史结果逐位可复现）。已登记为 DRIVERS.md **G-4**。

---

## 10. D15 — 真·多 seed，G-1 闭合

口径：rx-only, K=4, **lr=5e-4**, `--shuffle`, seeds {1, 7, 42, 2024}。来源 `cache/d15_multiseed_summary.json`。

| 架构 | s1 | s7 | s42 | s2024 | mean | **std** |
|------|----|----|-----|-------|------|---------|
| **V1**（纯 K 专家） | .77336 | .77369 | .77344 | .77326 | **0.77344** | **0.00018** |
| **V2**（shared + soft） | .75771 | .75748 | .75998 | .75498 | 0.75754 | 0.00205 |

| 统计量 | 值 |
|--------|----|
| mean diff (V1 − V2) | **+0.0159** |
| pooled std | 0.00146 |
| **Cohen's d** | **10.93** |
| **Welch t**（df≈6） | **15.45** |

**裁决：V1 > V2 极度显著，不是单 seed 偶然。** 另有两点：

- **V2 的 seed 方差是 V1 的 ~12 倍**（0.00205 vs 0.00018）。稀疏/共享路由不仅更差，**还更不稳定** —— 路由决策对数据顺序敏感。
- 开 shuffle 后 V1 从 0.7750 略降到 0.77344，V2 从 0.7558 升到 0.75754；**结论方向不变**。

---

## 11. D16（进行中）— 在方差口径下复核两大结论

| # | tag | 卡 | 检验 |
|---|-----|----|------|
| 1-3 | `d16_v1_lr1e3_s{42,1,7}` | 0 | LR 峰值：lr=5e-4 是否显著优于 1e-3（对照 §10 的 V1 组） |
| 4-6 | `d16_v2_tk2_s{42,1,7}` | 1 | 路由：top_k=2 是否显著劣于 top_k=4（对照 §10 的 V2 组） |

调度 `cache/d16_chain.sh <device>`。
